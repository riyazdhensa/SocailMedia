<link rel="stylesheet" href="/css/comments.scss">

<li>
    <p>
  
        <%= comment.content %>
        <% if(locals.user && locals.user.id == comment.user.id) {  %>

            <small class="commetns-delete-button">
                <a class="delete-comment-button" href="/comment/destroy/<%= comment.id%>" >X</a>
            </small>

            <% } %>
        <br />
        <small>
            <%= comment.user.name %>
        </small>
    </p>
</li>
